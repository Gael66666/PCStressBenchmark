<Window x:Class="PCStressBenchmarkGUI.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historique des tests" Height="560" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="#0F1117"
        FontFamily="Segoe UI"
        ResizeMode="CanResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Historique des tests" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="0" Text="Évolution de tes scores au fil du temps" FontSize="13" Foreground="#8A8A94" Margin="0,30,0,16" VerticalAlignment="Top"/>

        <!-- Mini graphique d'évolution -->
        <Border Grid.Row="1" Background="#1A1C25" CornerRadius="12" Padding="16" Margin="0,0,0,16" Height="140">
            <Canvas x:Name="ChartCanvas" ClipToBounds="True"/>
        </Border>

        <Border Grid.Row="2" Background="#1A1C25" CornerRadius="12" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="HistoryList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#22242F" CornerRadius="8" Padding="12" Margin="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding DateLabel}" Foreground="#8A8A94" FontSize="11"/>
                                        <TextBlock Text="{Binding SummaryLabel}" Foreground="White" FontSize="13" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding ScoreLabel}" Foreground="#7C5CFC" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding Tier}" Foreground="#8A8A94" FontSize="11" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
